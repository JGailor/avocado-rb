require "spec_helper"

RSpec.describe Avocado::Rb do
  it "has a version number" do
    expect(Avocado::Rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
